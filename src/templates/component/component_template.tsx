import type { FC } from "react";

export type $pascal_name$Props = {};

export const $pascal_name$: FC<$pascal_name$Props> = (props) => {
  const {} = props;

  return (
    <div>
      $pascal_name$ component works!
    </div>
  );
};
